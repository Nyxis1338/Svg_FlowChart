## 四、当前已实现和待优化项

| 功能                      | 状态        | 说明                                 |
| ------------------------- | ----------- | ------------------------------------ |
| 新建连线（`output` 拖拽） | ✅ 已实现   | 支持 `straight`/`bezier`/`flowchart` |
| `input` 锚点重连          | ✅ 已实现   | 有连线时可拖拽重连                   |
| `both` 锚点               | ⚠️ 部分实现 | 可作为源端拖拽，但示例未使用         |
| 法线方向 stub             | ✅ 已实现   | 拖拽时第一段沿法线延伸               |
| 目标锚点已有连线          | ✅ 已实现   | 新建和重连都允许选择                 |
| 鼠标与虚线端点对齐        | ⚠️ 接近对齐 | 偶有小偏差，需检查坐标转换           |
| 最大连线数量控制          | ❌ 未实现   | 可扩展                               |
| 正式连线 stub             | ⚠️ 未实现   | 正式连线未传递 `orientation`         |

---

## 五、后续优化建议

1. **正式连线 stub**：在 `Store.computeConnectionPath` 中，根据 `sourceAnchorId` 获取锚点 `position`，计算法线方向，传递给 `generatePath`，使正式连线与拖拽预览一致。
2. **最大连线数量控制**：在 `addConnection` 或 `queryAnchorUnderMouse` 中增加校验，限制每个锚点的最大连线数。
3. **坐标精度**：检查 `screenToCanvas` 和 `calcAnchorPosForNode` 的浮点精度，减少微小偏差。

如果需要更详细的某个环节代码，请告诉我具体位置。

# 图层

当节点上下叠放时，所有节点的锚点在上，出现图示遮罩 还是建议zindex方式。

你的思考非常全面且系统，覆盖了从底层坐标计算、交互逻辑、视觉样式到工程化的全链路优化。这些方向都非常有价值，我逐条给出分析和建议，并附上实施方案。

---

### 1. 坐标计算统一与连线分段（gap+stub+connection+stub+gap）

**你的想法**：将连线拆分为 `gap + stub + connection + stub + gap`，分别计算起点和终点位置。

**我的建议**：

- 这个设计完全正确，且与 jsPlumb 的 `gap` 和 `stub` 概念一致。**当前代码已有 `gap` 和 `stub` 参数，但应用方式不够统一**（`applyGap` 和 `connectorFlowchart` 中分别处理，且 `gap` 只用于偏移起点终点，未在路径中显式分段）。
- **实施方案**：
  1. 在 `Store.computeConnectionPath` 中，先计算锚点坐标（原始端点），然后应用 `gap`（偏移端点），再应用 `stub`（生成拐点）。
  2. 将 `gap` 和 `stub` 作为独立的线段添加到路径中，而不是只偏移坐标。例如，对于 `straight` 连线，路径应为：`M (start) L (start + stub) L (end - stub) L (end)`，中间为原始连线，两端为 stub。
  3. 这样 `gap` 和 `stub` 都在路径中可视化，便于调试和未来扩展（例如添加不同颜色区分）。
  4. **建议**：新建 `calc/connector/path-builder.ts`，统一处理所有连接器的 `gap`/`stub` 应用，避免在每个连接器中重复实现。

---

### 2. 静态锚点 stub 垂直边线引出（即使同水平线也要折线）

**你的想法**：对于四种节点形状（矩形/菱形/圆/椭圆），stub 段要垂直于边线引出；即使起点终点在同一水平线，也要走“∏”或“凵”形折线。

**我的建议**：

- 这是流程图正交连线的典型需求。当前 `connectorFlowchart` 已支持 stub，但**未强制垂直引出**（当水平距离很小时会退化）。
- **实施方案**：
  1. 在 `connectorFlowchart` 中，**强制第一段沿锚点法线方向延伸**（即 `orientation`），即使水平距离很小也要保留 stub 长度（`alwaysRespectStubs: true`）。
  2. 对于同一水平线的情况，`connectorFlowchart` 应生成 `M (start) -> (start.x + stub, start.y) -> (midX, start.y) -> (midX, end.y) -> (end.x - stub, end.y) -> end`，形成“∏”或“凵”形状。
  3. 需要修改 `connectorFlowchart` 的算法，**不再判断 `absDx < stub` 时直接画直线**，而是始终保留 stub 段（即使重叠）。
  4. **建议**：在 `connectorFlowchart` 中增加 `alwaysRespectStubs` 参数（默认 `true`），同时保持现有 `stub` 行为，可配置。

---

### 3. Continuous（连续）锚点拖拽创建连线

又出了点问题！

Continuous 可以创建，动态连线，但创建的4条连线重叠放在一起，是因为最短算法的原因。

### 4. 代码清理与模块拆分

### 5. 节点/连线/锚点图层引入 z-index

---

### 7. API 应用与 Demo 工程化

**你的想法**：生成 `SvgFlow.umd.js`，并用 JS 引入方式制作各类 Demo。

**我的建议**：

- 这是 **工程化的关键一步**，有利于推广和测试。
- **实施方案**：
  1. **打包 UMD**：配置 Vite/Rollup，将库打包为 UMD 格式，入口为 `src/index.ts`（导出 `SvgEngine` 和所有类型）。
  2. **Demo 结构**：
     - `demo/basic/`：基础节点和连线。
     - `demo/anchors/`：锚点方向测试。
     - `demo/continuous/`：连续锚点拖拽。
     - `demo/theme/`：样式定制。
     - `demo/export-import/`：导入导出。
  3. **每个 Demo 独立 HTML**，使用 `<script src="../../dist/SvgFlow.umd.js">` 引入，配合简单的 JS 代码。
  4. **文档**：使用 `typedoc` 生成 API 文档，或手动编写 `README.md` 示例。

---

## 整体实施优先级建议

| 优先级 | 事项                                   | 理由         |
| ------ | -------------------------------------- | ------------ |
| **高** | 3. Continuous 锚点交互（基础功能缺失） | 影响用户体验 |

| **中** | 1. 坐标计算与连线分段 | 影响视觉精度，但当前可用 |
| **中** | 2. stub 垂直引出 | 优化流程图外观 |

| **低** | 7. Demo 工程化 | 当前不影响功能，但利于后续推广 |

---

## 总结

你的优化方向非常明确且合理。我建议**从 5（z-index）、6（样式 API）、3（continuous）开始**，因为它们是用户能直观感受到的改进，且影响面广。然后逐步推进底层算法（1、2）和工程化（4、7）。

如果你同意，我可以优先提供 **z-index 控制**和**样式 API** 的具体实现代码。如果还有其他疑问，欢迎继续讨论。
