当前项目的拖拽连线和锚点重连逻辑，经过多次重构和修复，已形成一个相对完整的状态机驱动体系。以下是对这两个核心交互的完整梳理。

---

## 一、拖拽连线逻辑（创建新模式）

### 1. 触发条件

- 用户在**静态锚点（`STATIC`）** 上按下鼠标左键。
- 锚点的 `direction` 必须为 `output` 或 `both`（`input` 不能作为源端）。
- 锚点未被占用（`findConnectionByAnchor` 返回 `undefined`）。

### 2. 状态机流程

```
IDLE → LINK_DRAGGING → (HOVERING) → IDLE / CONNECTED
```

### 3. 详细步骤

#### 步骤 1：`startLinkDrag`（mousedown 触发）

- 检查 `viewport.isSpaceActive()`（空格平移时禁止拖拽）。
- 校验锚点方向（`output` 或 `both`）。
- 获取锚点的逻辑坐标（`calcAnchorPosForNode`）作为起点。
- 获取锚点的法线方向（`getAnchorOrientation`），用于后续 `flowchart` 的 stub 计算。
- 创建 `linkDragData` 对象，存储起点坐标、类型（`create`）、法线方向等信息。
- 将状态切换为 `LINK_DRAGGING`，光标改为 `grabbing`。

#### 步骤 2：`processMove`（mousemove 驱动，通过 requestAnimationFrame 节流）

- 将鼠标屏幕坐标转换为画布逻辑坐标（`screenToCanvas`）。
- 更新 `linkDragData.endX/Y` 为当前鼠标位置。
- 调用 `renderer.setTempLine` 绘制临时连线：
  - 如果连接器类型为 `FLOWCHART`，调用 `connectorFlowchart(start, end, orientation, stub)`，生成带法线方向 stub 的正交折线。
  - 如果为 `BEZIER`，生成贝塞尔曲线。
  - 如果为 `STRAIGHT`，生成直线。
- 调用 `queryAnchorUnderMouse` 检测鼠标下方是否有可用的目标锚点：
  - 排除自身锚点（不能连到自己）。
  - 新建模式下，**允许选择已有连线的锚点**（jsPlumb 行为，允许多条连线连到同一锚点）。
- 如果命中有效目标：
  - 清除旧高亮，高亮目标锚点。
  - 状态切换为 `HOVERING`。
- 如果未命中：
  - 清除高亮，状态回到 `LINK_DRAGGING`。

#### 步骤 3：`onMouseUp`（mouseup 触发）

- 如果状态为 `LINK_DRAGGING` 或 `HOVERING`：
  - 调用 `queryAnchorUnderMouse` 获取最终目标锚点。
  - 如果目标锚点存在且不属于同一节点：
    - 检查是否已存在相同源-目标连线（查重）。
    - 如果不存在，调用 `store.addConnection` 创建新连线，使用默认样式（`Defaults.connection.stroke` 等）。
    - 设置 `success = true`。
  - 如果目标无效或查重失败，取消操作（不创建连线）。
  - 清除高亮，清除临时线，重置状态为 `IDLE`。

### 4. 临时连线的视觉反馈

- 新建模式：灰色虚线（`rgba(150,150,150,0.7)`），`stroke-dasharray: 8 4`，线宽 `2.5`。
- 拖拽端点：虚线末端有一个半透明圆点，跟随鼠标移动。

---

## 二、锚点重连逻辑（修改已有连线）

### 1. 触发条件

- 用户在**已被连线占用的锚点**上按下鼠标左键。
- 锚点的 `direction` 为 `output`、`input` 或 `both`（`input` 也可重连）。
- 连线自身允许重连（`conn.detachable !== false` 且 `conn.reattach !== false`）。
- 锚点允许拖拽（`anchor.connectionsDetachable !== false`）。

### 2. 状态机流程

```
IDLE → LINK_DRAGGING → (HOVERING) → IDLE / CONNECTED
```

（与新建模式共用状态机，但 `linkDragData.type = 'reconnect'` 标记重连模式）

### 3. 详细步骤

#### 步骤 1：`startLinkDrag`（mousedown 触发）

- 调用 `store.findConnectionByAnchor(anchor.id)` 检测锚点是否已被占用。
- 如果被占用：
  - 确定该锚点是连线的 `source` 端还是 `target` 端。
  - **计算固定端**：
    - 如果拖拽的是 `target` 端，固定 `source` 端。
    - 如果拖拽的是 `source` 端，固定 `target` 端。
  - 获取固定端锚点的逻辑坐标（作为临时线的起点）。
  - 获取固定端锚点的法线方向（`getAnchorOrientation`），用于 stub。
  - 记录原连线的样式（`stroke`, `strokeWidth`, `connectorType`）。
  - 创建 `linkDragData`，标记 `type: 'reconnect'`，存储固定端坐标、原连线信息、端点方向（`endpoint: 'source' | 'target'`）。
  - **隐藏原连线**：调用 `renderer.setReconnecting(conn.id, true)`，将该连线 ID 加入 `reconnectingIds` 集合，`ConnectionRenderer.render` 会跳过该 ID 的渲染。
  - 高亮被拖拽的锚点。
  - 状态切换为 `LINK_DRAGGING`。

#### 步骤 2：`processMove`（mousemove 驱动）

- 与新建模式类似，但临时线起点为**固定端锚点**，终点为鼠标位置。
- 临时线样式使用原连线的颜色和线宽（`stroke` 和 `strokeWidth`），实线（`stroke-dasharray: 'none'`）。
- 目标锚点检测逻辑与新建模式相同，但**允许选择已有连线的锚点**（可连接到已被其他连线占用的锚点）。

#### 步骤 3：`onMouseUp`（mouseup 触发）

- 如果状态为 `LINK_DRAGGING` 或 `HOVERING`：
  - 调用 `queryAnchorUnderMouse` 获取最终目标锚点。
  - 如果目标锚点存在且不属于同一节点：
    - 根据 `endpoint` 字段确定要更新的端：
      - `endpoint === 'source'`：更新 `sourceAnchorId` 为目标锚点。
      - `endpoint === 'target'`：更新 `targetAnchorId` 为目标锚点。
    - 查重（检查是否已存在相同源-目标连线）。
    - 如果不存在，调用 `store.updateConnection` 更新连线。
    - 设置 `success = true`。
  - 如果目标无效或查重失败：
    - **不恢复原连线**（数据未变，连线已隐藏，但渲染器会重新显示）。
  - **恢复显示原连线**：调用 `renderer.setReconnecting(conn.id, false)`。
  - 清除高亮，清除临时线，重置状态为 `IDLE`。

### 4. 重连的视觉反馈

- 原连线消失（被隐藏）。
- 临时线使用原连线颜色（实线），从固定端到鼠标位置。
- 被拖拽锚点高亮（放大、变色）。
- 目标锚点悬停时高亮。

---

## 三、关键辅助方法

| 方法                                 | 作用                                                                     |
| ------------------------------------ | ------------------------------------------------------------------------ |
| `getAnchorOrientation(anchor)`       | 根据 `position` 返回法线方向 `{dx, dy}`，用于 `flowchart` 的 stub 计算。 |
| `queryAnchorUnderMouse(evt)`         | 检测鼠标下方最近的有效锚点，排除自身（重连模式/新建模式均排除自身）。    |
| `calcAnchorPosForNode(node, anchor)` | 根据节点形状和锚点类型计算锚点逻辑坐标。                                 |
| `screenToCanvas(point)`              | 屏幕坐标 → 画布逻辑坐标（ViewportManager 提供）。                        |
| `setReconnecting(connId, bool)`      | 控制原连线隐藏/显示（SvgRenderer 提供）。                                |
| `setTempLine(...)`                   | 绘制临时连线，支持 `orientation` 和 `stub`（SvgRenderer 提供）。         |

---

## 四、当前已实现和待优化项

| 功能                      | 状态        | 说明                                 |
| ------------------------- | ----------- | ------------------------------------ |
| 新建连线（`output` 拖拽） | ✅ 已实现   | 支持 `straight`/`bezier`/`flowchart` |
| `input` 锚点重连          | ✅ 已实现   | 有连线时可拖拽重连                   |
| `both` 锚点               | ⚠️ 部分实现 | 可作为源端拖拽，但示例未使用         |
| 法线方向 stub             | ✅ 已实现   | 拖拽时第一段沿法线延伸               |
| 目标锚点已有连线          | ✅ 已实现   | 新建和重连都允许选择                 |
| 鼠标与虚线端点对齐        | ⚠️ 接近对齐 | 偶有小偏差，需检查坐标转换           |
| 最大连线数量控制          | ❌ 未实现   | 可扩展                               |
| 正式连线 stub             | ⚠️ 未实现   | 正式连线未传递 `orientation`         |

---

## 五、后续优化建议

1. **正式连线 stub**：在 `Store.computeConnectionPath` 中，根据 `sourceAnchorId` 获取锚点 `position`，计算法线方向，传递给 `generatePath`，使正式连线与拖拽预览一致。
2. **最大连线数量控制**：在 `addConnection` 或 `queryAnchorUnderMouse` 中增加校验，限制每个锚点的最大连线数。
3. **坐标精度**：检查 `screenToCanvas` 和 `calcAnchorPosForNode` 的浮点精度，减少微小偏差。

如果需要更详细的某个环节代码，请告诉我具体位置。

图层
当节点上下叠放时，所有节点的锚点在上，出现图示遮罩 还是建议zindex方式。
