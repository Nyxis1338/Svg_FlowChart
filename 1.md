### 7. API 应用与 Demo 工程化

**你的想法**：生成 `SvgFlow.umd.js`，并用 JS 引入方式制作各类 Demo。

**我的建议**：

- 这是 **工程化的关键一步**，有利于推广和测试。
- **实施方案**：
  1. **打包 UMD**：配置 Vite/Rollup，将库打包为 UMD 格式，入口为 `src/index.ts`（导出 `SvgEngine` 和所有类型）。
  2. **Demo 结构**：
     - `demo/basic/`：基础节点和连线。
     - `demo/anchors/`：锚点方向测试。
     - `demo/continuous/`：连续锚点拖拽。
     - `demo/theme/`：样式定制。
     - `demo/export-import/`：导入导出。
  3. **每个 Demo 独立 HTML**，使用 `<script src="../../dist/SvgFlow.umd.js">` 引入，配合简单的 JS 代码。
  4. **文档**：使用 `typedoc` 生成 API 文档，或手动编写 `README.md` 示例。

---

## 整体实施优先级建议

| **低** | 7. Demo 工程化 | 当前不影响功能，但利于后续推广 |
